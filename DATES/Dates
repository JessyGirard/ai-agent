# Session log — 2026-04-23 (Cursor / ai-agent workspace)

This file records work done in **this Cursor session** with the coding assistant (Composer).

### Who is in this chat?

There is **no separate “ChatGPT” participant** in this thread. When people say “ChatGPT” they often mean **the AI replying in Cursor** (here: the coding assistant). The back-and-forth is **you ↔ Cursor’s assistant**, unless you pasted text that came from another app.

### Full request log and where replies live

- **Every user request** from this same long session is captured verbatim (74 messages, in order) in:  
  `DATES/Dates_user_requests_appendix.md`
- **Assistant replies** (including diagnostics, evaluations, and tool use) are stored in Cursor’s agent transcript for this chat. Cursor may redact some tool payloads as `[REDACTED]`. Transcript id: `f9b945fd-85ec-447f-9bd1-aab18d9923f9` (parent chat reference: [Session transcript](f9b945fd-85ec-447f-9bd1-aab18d9923f9)).

**Note:** There is no access to conversations that happened only in the **ChatGPT website/app** outside this workspace unless you pasted them here.

---

## 1. Work completed (increments / code)

### Increment 10 — Duplicate Step 1 cursor stability
- **Problem:** After showing step 1 twice, “Continue” could jump past step 2 (e.g. step 3).
- **Fix:** `playground.append_recent_answer_history`: when a new multi-step frame is stored and cursor is reset to 0, also reset `last_rendered_step_index` to 0 so anchors stay aligned.
- **Tests:** `test_append_stores_multi_step_frame_resets_last_rendered_with_cursor_increment_10`, `test_duplicate_reanchor_step_one_then_continue_next_increment_10`.

### Increment 11 — One-by-one entry alignment
- **Problem:** Long prompts with stray `step|number` before “only include number 1” could bind the wrong index.
- **Fix:** `_one_by_one_sequence_entry_target_1based` in `journal_service.py`, inserted before generic `(?:step|number)` branch.
- **Tests:** `test_one_by_one_entry_only_include_anchors_before_stray_step_number_increment_11`.

### Increment 12 — Cursor / last_rendered synchronization
- **Fix:** `prompt_builder.py` indexed writeback: `last_rendered_step_index` tracks displayed `target_idx`; cursor aligned (later tightened further).
- **Fix:** `resolve_sequence_step_navigation` Continue/Next: advance from `last_rendered_step_index + 1` only.
- **Tests:** `test_increment_12_cursor_matches_last_rendered_after_each_indexed_step`.

### Increment 13 — Indexed frame selection vs short aside
- **Problem:** Bare Continue/Next could bind a short numbered assistant aside instead of the full list.
- **Fix:** `lookup_steps_for_matched_answer` — prefer longest indexed frame; bare Continue/Next override when a longer frame exists.
- **Tests:** `test_increment_13_critical_endpoints_list_frame_and_next_after_parasite`.

### Increment 14 — Duplicate re-anchor / cursor sync
- **Fix:** Indexed writeback: when `new_lr is not None`, align cursor with displayed step (explain exception existed then; later removed in stabilization).

### Increment 16 — DEBUG_SEQUENCE instrumentation
- **Add:** `services/prompt_builder.py` — `DEBUG_SEQUENCE` flag / env, `_emit_sequence_debug_turn`, stderr `[DEBUG]` block per `build_messages` turn (default off).

### Stabilization — language-switch anchor (no cursor fallback)
- **Change:** `resolve_sequence_step_navigation` language branch: anchor no longer falls back to `cursor_before` for position; then `return anchor, anchor, anchor` for `new_lr` so writeback updates `last_rendered`.

### Stabilization — explain-step cursor exception removed
- **Change:** `prompt_builder.py` indexed writeback: when `new_lr is not None`, always `set_last_rendered_step_index(target_idx)` and `set_sequence_step_cursor(target_idx)` (removed explain-only cursor divergence).

### Test updates
- `test_language_switch_uses_last_rendered_step_not_cursor`: expectation updated so after “Explain step 2.” cursor == 2 (matches displayed step).
- `test_increment_16_exact_transcript_one_by_one_start_continue_next_regression`: exact 5-turn transcript (API steps → one-by-one → Start with 1 → Continue → Next) with final cursor/lr == 3.

### Other session actions
- Created folder **`DATES/`** and file **`Dates`** (this file) at user request.
- User asked not to execute before discussing — acknowledged process gap (spec read as “implement”).
- Live runs with `DEBUG_SEQUENCE=1`: verbatim transcript reproduction (no skip in that run).
- Focused post-language-switch tests: Step 4 may re-render on language switch; Continue advances to Step 5 (PASS in harness; Step 4 can still appear twice by design when re-translating same anchor).

---

## 2. Diagnostics & evaluations (read-only / advice)

### Alignment audit (invariant)
- Stated goal: `last_rendered_step_index` single source of truth; cursor mirrors after displayed step.
- Iterations: language-switch cursor fallback removed; explain exception removed; language branch returns `(anchor, anchor, anchor)`.
- Final verdict in session: **aligned** for sequence-position truth after those changes, with resets documented (`clear_recent_answer_session`, append multi-frame reset).

### “Is my project in danger?” / “Can I trust ChatGPT?” / success odds
- Honest framing: not dead; risk is routing/state edge cases; stabilization + regressions reduce risk; “very good” achievable with continued hardening.

### Stabilization sprint advice (summary)
- Freeze features briefly; invariant tests; DEBUG traces on curve balls; avoid broad regex changes without tests.

---

## 3. Key files touched (reference)

| Area | File |
|------|------|
| Sequence resolve | `services/journal_service.py` |
| Prompt / indexed inject / debug | `services/prompt_builder.py` |
| Session state / append | `playground.py` |
| Regressions | `tests/run_regression.py` |

---

## 4. Commands / checks run (representative)

- `pytest` subsets for: sequence, indexed, language_switch, increment_12/13/14/16, duplicate_reanchor, one_by_one_entry, next_after_continue, etc. — **passing** after final stabilization steps in session.
- Live Python harness: `playground.build_messages` + `append_recent_answer_history` loops with `DEBUG_SEQUENCE=1` for transcript and step-line output.

---

## 5. Limitations (what this file does NOT contain)

- Chats that exist **only** in the external ChatGPT product (not opened in Cursor / not pasted here).
- A duplicate copy of every **assistant** message inside `Dates` itself — use `Dates_user_requests_appendix.md` plus the transcript id above for the full thread.

---

## 6. Optional next steps (not executed here)

- Broader `pytest tests/run_regression.py` full run if desired.
- Commit message grouping increments 10–16 + stabilization + Dates log.

---

## 7. Addendum: work completed after this file's first draft

### Increments delivered after the initial log
- **Increment 1B:** one-line progression + language (`Continue/Next en francais|in French|in English`) routed without changing progression math.
- **Increment 1C:** frame-retention stress transcript locked; indexed frame anchoring under language/step pivots verified.
- **Increment 1D:** French hard-lock edge coverage (variants/noise/mid-sentence/constraints/repeated switching) with minimal parser/routing guards.
- **Increment 2 / 2B / 2D / 2E / 2F / 2G / 2H:** step quality, operator-focused workflow contract, anti-repeat elaboration, tightened 4-6 full-list, explicit final regression step wording, no duplicate final-step echo, and hard no-postscript rule.
- **Increment 3A:** isolated Brave integration slice (`get_brave_api_key`, `tools/brave_search.py`, `tests/run_brave_search_smoke.py`) with live 200 verification.
- **Increment 3B:** Brave tool wired into parser/dispatcher (`TOOL:brave_search <query>`) and routed through existing post-tool flow.
- **Increment 3C:** stronger prompt enforcement for explicit web-search intent (`search the web|find|look up`).
- **Increment 3D:** routing-layer hard enforcement for explicit web-search intent before first LLM pass (forced Brave call).
- **Increment 3E:** Brave source-backed answer contract (Summary + Sources, 1-3 sources, no-source fallback, language-preserving URLs).
- **Increment 3F:** source integrity (title - URL only), concise/clean summary clarity rules, and missing-URL source suppression.

### Broad regression sweep and baseline gate
- Ran full suite: `python -m pytest tests/run_regression.py -v --tb=short`.
- Initial broad run: **705 passed, 2 failed** (`test_blank_input`, `test_system_eval_case_expected_status_fields_absent_behavior_unchanged`).
- Minimal corrective action chosen: **test updates only** (behavior-intent alignment), no unrelated code changes.
- Re-ran full suite: **707 passed, 0 failed**.
- Baseline verdict: current state is acceptable as a **stability baseline release point**.

### Brave API runner verification
- Ran one Tool-1/system-eval live case against `GET https://api.search.brave.com/res/v1/llm/context?q=what%20is%20an%20API`.
- Assertions: status 200, JSON response, non-empty body.
- Outcome: **PASS** with grounding/context payload returned; key remained masked in reported summaries.

---

*End of session log for 2026-04-23 (Cursor).*
